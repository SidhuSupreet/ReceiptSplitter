{
  "name": "expense-splitter-ocr-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that proxies receipt OCR text to Anthropic Claude, gated by Google ID token + email allowlist.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260504.1",
    "typescript": "~5.6.0",
    "wrangler": "^4.87.0"
  }
}
